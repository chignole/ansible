---

- hosts: all
  become: true
  tasks:

  - name: update & upgrade
    apt:
      update_cache: yes
      upgrade: dist

  - name: create mounted directories
    ansible.builtin.file:
      path: /mnt/share
      state: directory
      mode: '0755'
